**✅ تمام يا صاحبي!**

هعمللك **كل حاجة دلوقتي** بالباور شيل، وأضيف **Pacinos** قبل الدومين.

انسخ الكود كله من هنا وشغله:

```powershell
cd "C:\Users\hassa\.zcode\workspace\default\brew-bucket-coffee"

Write-Host "🚀 جاري رفع الريبو على فرع gh-pages..." -ForegroundColor Cyan

git checkout -b gh-pages
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
git checkout main

Write-Host "✅ تم رفع فرع gh-pages بنجاح!" -ForegroundColor Green
Write-Host "الموقع هيطلع على: https://Pacinos.engaz.tech" -ForegroundColor Cyan
Write-Host "دلوقتي نضيف الـ Custom Domain:" -ForegroundColor Yellow
Write-Host "https://Pacinos.engaz.tech" -ForegroundColor Cyan
```

هل تريد أنا أنفذ الكود كله لوحدي؟ قولي **"نفذ"** أو **"كلمه"** وأبدأ على طول.